<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="Item">

    <resultMap id="ItemMap" type="Item">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="book_id" property="bookId"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="image" property="image"/>
        <result column="image_id" property="imageId"/>
        <result column="date" property="date"/>
        <result column="note" property="note"/>
        <result column="consume_type" property="consumeType"/>
        <result column="consume_type_name" property="consumeTypeName"/>
        <result column="amount" property="amount"/>
        <result column="longitude" property="longitude"/>
        <result column="latitude" property="latitude"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        items    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        `user_id`,`book_id`,`type`,`status`,`image`,`date`,`note`,`consume_type`,`amount`,`longitude`,`latitude`,`created_at`,`updated_at`    </sql>

    <sql id="vals">
        #{userId},#{bookId},#{type},#{status},#{image},#{date},#{note},#{consumeType},#{amount},#{longitude},#{latitude},now(),now()    </sql>

    <insert id="create" parameterType="Item" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="ItemMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>


    <update id="update" parameterType="Item">
        UPDATE
        <include refid="tb"/>
        <set>
            updated_at = now()
            <if test="userId != null" > ,`user_id` = #{userId}</if>
            <if test="bookId != null" > ,`book_id` = #{bookId}</if>
            <if test="type != null" > ,`type` = #{type}</if>
            <if test="status != null" > ,`status` = #{status}</if>
            <if test="image != null" > ,`image` = #{image}</if>
            <if test="date != null" > ,`date` = #{date}</if>
            <if test="note != null" > ,`note` = #{note}</if>
            <if test="consumeType != null" > ,`consume_type` = #{consumeType}</if>
            <if test="amount != null" > ,`amount` = #{amount}</if>
            <if test="longitude != null" > ,`longitude` = #{longitude}</if>
            <if test="latitude != null" > ,`latitude` = #{latitude}</if>
            <if test="createdAt != null" > ,`created_at` = #{createdAt}</if>
        </set>
        WHERE id=#{id}
    </update>


    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="findByUserId" parameterType="long" resultMap="ItemMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE user_id = #{userId}
    </select>


</mapper>