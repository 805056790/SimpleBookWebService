<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="Book">

    <resultMap id="BookMap" type="Book">
        <id column="id" property="id"/>
                    <result column="user_id" property="userId"/>
                    <result column="type" property="type"/>
                    <result column="name" property="name"/>
                    <result column="is_default" property="isDefault"/>
                    <result column="status" property="status"/>
                    <result column="budget" property="budget"/>
                    <result column="settlement_time" property="settlementTime"/>
                    <result column="id_remind" property="idRemind"/>
                    <result column="color" property="color"/>
                    <result column="store_type" property="storeType"/>
                    <result column="created_at" property="createdAt"/>
                    <result column="updated_at" property="updatedAt"/>
            </resultMap>

    <sql id="tb">
        books    </sql>

    <sql id="cols_all">
      id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        `user_id`,`type`,`name`,`is_default`,`status`,`budget`,`settlement_time`,`id_remind`,`color`,`store_type`,`created_at`,`updated_at`    </sql>

    <sql id="vals">
        #{userId},#{type},#{name},#{isDefault},#{status},#{budget},#{settlementTime},#{idRemind},#{color},#{storeType},now(),now()    </sql>

    <insert id="create" parameterType="Book" keyProperty="id" useGeneratedKeys="true">
      INSERT INTO
      <include refid="tb" />
      (<include refid="cols_exclude_id" />)
      VALUES
      (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="BookMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>


    <update id="update" parameterType="Book">
        UPDATE
        <include refid="tb"/>
        <set>
            updated_at = now()
                                            <if test="userId != null" > ,`user_id` = #{userId}</if>
                                                           <if test="type != null" > ,`type` = #{type}</if>
                                                           <if test="name != null" > ,`name` = #{name}</if>
                                                           <if test="isDefault != null" > ,`is_default` = #{isDefault}</if>
                                                           <if test="status != null" > ,`status` = #{status}</if>
                                                           <if test="budget != null" > ,`budget` = #{budget}</if>
                                                           <if test="settlementTime != null" > ,`settlement_time` = #{settlementTime}</if>
                                                           <if test="idRemind != null" > ,`id_remind` = #{idRemind}</if>
                                                           <if test="color != null" > ,`color` = #{color}</if>
                                                           <if test="storeType != null" > ,`store_type` = #{storeType}</if>
                                                           <if test="createdAt != null" > ,`created_at` = #{createdAt}</if>
                                                              </set>
        WHERE id=#{id}
    </update>


    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>


</mapper>